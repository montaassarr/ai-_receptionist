# Frontend Architecture Plan

## Scope
- Map the Next.js App Router structure in `app/`.
- Trace shared state and app wiring through `components/providers.tsx`, `contexts/`, and `hooks/`.
- Summarize what the frontend is using and how the pieces connect.

## Notes
- Step 3 from the earlier plan is intentionally skipped for now.
- This folder will hold the investigation notes and architecture summary.
- `01-route-map.md` captures the route tree and nested pages.
- `02-shared-state.md` captures provider, auth, config, and tenant flow.
- `03-api-layer.md` captures HTTP client and endpoint organization.
- `04-ui-system.md` captures the shared component system and styling approach.
- `05-architecture-brief.md` gives a concise end-to-end summary.
