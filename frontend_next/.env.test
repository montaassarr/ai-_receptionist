# Test Environment Configuration for Phase 4 E2E Tests
# Using admin credentials since tenant user creation requires tenant association

# Frontend URL
FRONTEND_URL=http://localhost:3000

# Backend API URL
NEXT_PUBLIC_API_URL=http://localhost:8000

# Test user credentials (using admin for now - has login access)
TEST_USER_EMAIL=admin
TEST_USER_PASSWORD=admin123

# Test tenant info
TEST_TENANT_ID=test_tenant_001
TEST_TENANT_NAME=Test Tenant Company

# API keys for testing (use test/mock keys, not production)
TEST_GROQ_KEY=test_groq_key

# Test timeouts (milliseconds)
TEST_TIMEOUT=30000
PAGE_LOAD_TIMEOUT=10000
